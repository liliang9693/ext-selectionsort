let str = ' 00 02 03 05 FF';

// if (c.length > num)
// c = c.slice(c.length - num)
let a = Buffer.from('123');
let b = Buffer.from('23')

console.log(a,b,b.compare(a))
// console.log(str.split(' ').filter(item => item).map(item => parseInt(item, 16)))

